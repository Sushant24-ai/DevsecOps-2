pipeline{
    agent(any)
    stages{
        stage("Build Artifact-1"){
            steps{
                sh "apt-get install maven"
                /*archive "target/*.jar"*/
            }
        }

    }
}
