pipeline{
    agent(any)
    stages{
        stage("Build Artifact-1"){
            steps{
                echo "pipeline is working-1"
                sh ""
                /*archive "target/*.jar"*/
            }
        }

    }
}
