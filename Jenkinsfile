pipeline{
    agent(any)
    stages{
        stage("Build Artifact-2"){
            steps{
                echo "pipeline is working-1"
                sh ""
                /*archive "target/*.jar"*/
            }
        }

    }
}
