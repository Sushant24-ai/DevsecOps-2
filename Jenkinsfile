pipeline{
    agent(any)
    stages{
        stage("Build Artifact-6"){
            steps{
                echo "pipeline is working-1"
                sh ""
                /*archive "target/*.jar"*/
            }
        }

    }
}
