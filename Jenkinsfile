pipeline{
    agent(any)
    stages{
        stage("Build Artifact-1"){
            steps{
                echo "pipeline is working"
                sh ""
                /*archive "target/*.jar"*/
            }
        }

    }
}
